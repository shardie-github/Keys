{
  "name": "keys",
  "version": "1.0.0",
  "description": "The keyring to modern tools - A marketplace of structured assets that unlock practical capability in Cursor, Jupyter, GitHub, Stripe, and more",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "lint:frontend": "cd frontend && npm run lint",
    "lint:backend": "cd backend && npm run lint",
    "format:check": "npx prettier --check \"ENV_VARS.md\" \"outputs/smoke_report.md\" \"outputs/smoke_report.json\"",
    "type-check": "npm run type-check:frontend && npm run type-check:backend",
    "type-check:frontend": "cd frontend && npm run type-check",
    "type-check:backend": "cd backend && npm run type-check",
    "db:verify": "tsx scripts/db-verify.ts",
    "db:smoke": "tsx scripts/db-smoke.ts",
    "db:migrate": "cd backend && npm run migrate",
    "db:reconcile": "echo 'Run migration: backend/supabase/migrations/00000000000000_backend_contract_reconcile.sql in Supabase SQL editor'",
    "clean": "tsx scripts/clean.ts",
    "doctor": "tsx scripts/doctor.ts",
    "verify": "tsx scripts/verify.ts",
    "keys": "python scripts/keys_cli.py",
    "keys:doctor": "npm run lint && npm run type-check",
    "keys:check": "npm run lint && npm run type-check && npm --workspace backend run test && npm --workspace frontend run test",
    "keys:smoke": "python scripts/run_notebooks_smoke.py --timeout 300",
    "keys:audit": "detect-secrets scan --baseline .secrets.baseline && npm --workspace backend audit --audit-level=high && npm --workspace frontend audit --audit-level=high"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "typescript": "^5.9.3",
    "use-isomorphic-layout-effect": "^1.2.1",
    "use-sync-external-store": "^1.6.0"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0",
    "concurrently": "^8.2.2",
    "pg": "^8.16.3"
  },
  "overrides": {
    "@next/eslint-plugin-next": {
      "glob": "10.5.0"
    },
    "vite": "7.3.1",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "pnpm": {
    "overrides": {
      "@next/eslint-plugin-next>glob": "10.5.0",
      "vite": "7.3.1"
    }
  }
}
