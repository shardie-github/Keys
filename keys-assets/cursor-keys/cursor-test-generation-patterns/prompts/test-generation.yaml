# Cursor Keys: Test Generation Patterns - Mega Prompt
# Unlocks test generation patterns in Cursor

---
name: Test Generation
description: Generate comprehensive tests including unit tests, integration tests, test data, mocks, and coverage patterns
version: 1.0.0

# Instructions
instructions: |
  Generate comprehensive tests with:
  1. Unit tests for functions/components
  2. Integration tests for APIs/workflows
  3. Test data generators
  4. Mock patterns
  5. Coverage goals

# Examples
examples:
  - name: "Unit Test Example"
    code: |
      import { describe, it, expect, vi } from 'vitest';
      import { functionToTest } from './module';

      describe('functionToTest', () => {
        it('should handle valid input', () => {
          const result = functionToTest('valid');
          expect(result).toBe('expected');
        });

        it('should handle invalid input', () => {
          expect(() => functionToTest(null)).toThrow();
        });
      });
